# Copy to .env locally if needed. Never commit real values.
# Miner supplies these at Lium / worker deploy time.

# Required for pre-train LLM rules gate
OPENROUTER_API_KEY=

# Optional smoke override (does not change egalitarian offset pin identity)
# PRISM_RECIPE_TOKEN_BUDGET=1000000

# Lium / Base worker deploy (host-side; do not bake into image)
# LIUM_API_KEY=
# BASE_API_URL=https://chain.joinbase.ai
