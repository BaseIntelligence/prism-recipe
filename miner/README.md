# Placeholder so default docker builds have a miner/ context path.
# Real miner code is staged here by prism_recipe.build_context.assemble_build_context.
# Do not put harness/rules/Dockerfile here — build-prep rejects sealed collisions.
