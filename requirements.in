# Direct runtime + image-path deps (torch is platform-specific; see requirements-torch-cpu.lock).
httpx>=0.27
pydantic>=2.8
pydantic-settings>=2.4
huggingface_hub>=0.24
PyYAML>=6.0
numpy>=1.26
tiktoken>=0.7
pytest>=8
pytest-asyncio>=0.23
ruff>=0.6
# Build backend for non-editable `pip install --no-deps .` in Dockerfiles.
hatchling>=1.25
